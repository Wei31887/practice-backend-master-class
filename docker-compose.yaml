version: "3.9"
services:
  # postgres:
  #   image: postgres:15-alpine
  #   ports:
  #     - "5433:5432"
  #   environment:
  #     - POSTGRES_USER=root
  #     - POSTGRES_PASSWORD=secret
  #     - POSTGRES_DB=simple_bank
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - DBSOURCE=postgres://root:secret@postgres:5433/simple_bank?sslmode=disable